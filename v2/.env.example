# ─── Frontend (Next.js) ──────────────────────────────────────
# Base URL for the Worker API (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8787

# ─── Worker API (set in wrangler.toml [vars] or via wrangler secret) ──
# CORS_ORIGIN=http://localhost:3000
# SESSION_TTL_SECONDS=86400
# MAX_USERS=50
# MAX_RECORDS_PER_TABLE=10000
# MAX_TABLES_PER_BASE=20
# RATE_LIMIT_REQUESTS_PER_MINUTE=60

# ─── Cloudflare (deploy only — GitHub Secrets) ──────────────
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ACCOUNT_ID=

# ─── Vercel (deploy only — GitHub Secrets) ───────────────────
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=
